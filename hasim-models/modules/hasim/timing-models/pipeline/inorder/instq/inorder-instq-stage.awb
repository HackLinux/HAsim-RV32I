%AWB_START

%name Inorder Instruction Queue Stage
%desc Inorder Instruction Queue Stage
%provides instq_stage

%attributes hasim model pipeline inorder

%public  inorder-instq-stage.bsv
%param IQ_NUM_SLOTS 4 "Number of slots in the instruction queue."

%AWB_END
