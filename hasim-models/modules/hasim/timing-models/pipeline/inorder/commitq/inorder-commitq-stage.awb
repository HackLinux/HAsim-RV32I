%AWB_START

%name Inorder Commit Queue Stage
%desc Inorder Commit Queue Stage
%provides commitq_stage

%attributes hasim model pipeline inorder

%public  inorder-commitq-stage.bsv

%param CQ_NUM_SLOTS 4 "Number of slots for the commit queue."

%AWB_END
