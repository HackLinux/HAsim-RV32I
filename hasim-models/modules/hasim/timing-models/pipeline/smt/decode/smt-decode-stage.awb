%AWB_START

%name SMT Decode/Issue Stage
%desc SMT Decode/Issue Stage
%provides decode_stage

%attributes hasim model pipeline inorder smt

%public  smt-decode-stage.bsv
%private decode.dic

%AWB_END
